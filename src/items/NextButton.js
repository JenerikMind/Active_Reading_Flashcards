

function NextButton({onClick}){
    return (
        <button 
        className="next btn btn-primary" 
        onClick={onClick}
        >
            Next
        </button>
    )
}

export default NextButton;